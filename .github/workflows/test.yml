name: Test

on:
  push:
    branches: [support-v1.12]
  pull_request:
    branches: [support-v1.12]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: "npm"
      - run: npm version
      - run: npm ci
      - run: npm run lint
      - run: npm run docs
      - run: git diff --exit-code
      - run: npm run license
      - run: npm run coverage
      - uses: codecov/codecov-action@v4
        with:
          files: ./coverage/clover.xml
